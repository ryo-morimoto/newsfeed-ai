# apps/web/wrangler.toml
name = "newsfeed-ai-web"
compatibility_date = "2026-01-22"
compatibility_flags = ["nodejs_compat"]

main = "@tanstack/react-start/server-entry"

# Environment variables are set via Cloudflare Dashboard or wrangler secret

# Production environment
# Custom domain can be added later via Cloudflare Dashboard:
# Workers & Pages → newsfeed-ai-web → Settings → Triggers → Custom Domains
[env.production]
